// src/config.ts

export const config = {
  adminPassword: 'p',
  winScore: 20,
  timeLimitSeconds: 45,
  playerLimit: 8,
  inactiveTimeout: 2,
  maxRounds: 2,
  gameRestartDelaySeconds: 20,
  autoStartDelayMs: 8000,
  resultDisplayDelayMs: 8000,
};
